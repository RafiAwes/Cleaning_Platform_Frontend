export enum tagTypes {
  profile = "profile",
  users = "users",
  categories = "categories",
  documents = "documents",
}

export const tagTypesList = [tagTypes.profile, tagTypes.users, tagTypes.categories, tagTypes.documents];